## Product spec (MVP)

See the attached plan/spec in the Cursor plan for the authoritative requirements.

This repository implements:

- Single-asset vault (Arbitrum `wstETH`)
- SignatureDepositQueue + SignatureRedeemQueue (trusted signer)
- Pendle strategy isolated in a Subvault guarded by call/verifier rules


